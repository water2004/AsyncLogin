{
  "schemaVersion": 1,
  "id": "asynclogin",
  "version": "${version}",
  "name": "AsyncLogin",
  "description": "Moves player DAT and usercache IO off the server tick thread.",
  "authors": ["EDTP"],
  "license": "MIT",
  "environment": "server",
  "entrypoints": {
    "main": ["org.edtp.asynclogin.platform.AsyncLoginMod"]
  },
  "mixins": ["asynclogin.mixins.json"],
  "depends": {
    "fabricloader": ">=0.17.3",
    "minecraft": ">=26.1 <26.2",
    "java": ">=25"
  }
}
